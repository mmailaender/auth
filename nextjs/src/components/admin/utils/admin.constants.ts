/** Path of the admin dashboard route. */
export const ADMIN_ROUTE = '/admin';

/** URL `?adminTab=` values for the admin dashboard tabs. */
export const TAB_USERS = 'users';
export const TAB_ORGANIZATIONS = 'organizations';
