collection Account {
    user: Ref<User>
    socialProvider: {
        name: "Github" | "Google" | "Facebook"
        userId: String
        email: String
    }?
    passkey: {
        publicKey: String
        algorithmId: Long
        id: String
    }?

    // unique [.user, .socialProvider?.name]
    // unique [.passkey?.id]
}