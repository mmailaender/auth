@role(server)
function verifySocialAccountExists(providerAccountId, provider) {
  Account.firstWhere((account) => {
    account.providerAccountId == providerAccountId
&& account.provider == provider
  }).exists()
};