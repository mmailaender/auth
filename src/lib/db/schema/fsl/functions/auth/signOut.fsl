function signOut() {
  let accessToken = Query.token()
  let user = Query.identity()

  // Delete Refresh Token
  accessToken!.data!.refresh.delete()
  // Delete Access Token
  accessToken!.delete()
}